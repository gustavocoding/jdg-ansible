# Java version must be a string supported from SDKMan
java_version: 8.0.275.open-adpt

# The server zip, relative to the files/server folder
server_zip: jboss-datagrid-7.1.0-server.zip

# The server config to use. For custom config, copy it first to ```servers/overlay/standalone/configuration```
custom_server_config: clustered.xml

# The zip containing the appdynamics agent, relative to the files/agent folder
agent_zip: AppServerAgent-20.10.0.31173.zip

# GC options for the server
gc_opts: -Xms8g -Xmx8g -XX:+UseG1GC -XX:InitiatingHeapOccupancyPercent=0 -XX:+DisableExplicitGC -XX:MaxGCPauseMillis=200 -XX:GCPauseIntervalMillis=5000

# Agent options for the server
agent_opts: -Djboss.modules.system.pkgs=org.jboss.byteman,com.singularity -javaagent:/opt/agent/javaagent.jar

# JFR options for the server
fr_opts: -XX:FlightRecorderOptions=stackdepth=128 -XX:StartFlightRecording=settings=profile,dumponexit=true,filename=server.jfr

# Cache name
cache_name: default

# Initial data loading
entries: 10000

# Sample data phrase size
phrase: 100

# HotRod protocol
protocol: 2.5

# Load batch
batch: 1000

# AWS specific variables
ssh-key: change